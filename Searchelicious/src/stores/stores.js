import { writable } from 'svelte/store';

// export const isNewSearch = writable(false);
export const searchDisplayAmount = writable(0);
export const searchTerm = writable('tomato');
// export const fetchedObjects = writable({});

// export const fetchedObjects = writable({provider: 'reddit', title: 'title'})
